apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: set
spec:
#  serviceName: svc
  replicas: 1
  selector:
    matchLabels:
      app: appset
  template:
    metadata:
      labels:
        app: appset
    spec:
      containers:
      - name: web
        image: nginx:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - name: storage
          mountPath: /usr/share/nginx
        - name: share
          mountPath: /tmp
      - name: db
        image: chrimson/sqlite3:latest
        command: ["/bin/sh", "-c", "tail -f /dev/null"]
        volumeMounts:
        - name: storage
          mountPath: /opt
        - name: share
          mountPath: /tmp
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: pvc
      - name: share
        emptyDir: {}
